#include "Absolute.h"
#include <cmath>
#include <iostream>

using namespace std;

Absolute::Absolute()
{

}

int Absolute::ab(int a)
{
    return abs(a);
}
double Absolute::ab(double a)
{
    return abs(a);
}
